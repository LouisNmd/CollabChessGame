import { ChessCasePipe } from './chess-case.pipe';

describe('ChessCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ChessCasePipe();
    expect(pipe).toBeTruthy();
  });
});
